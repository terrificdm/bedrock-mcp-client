[project]
name = "mcp-server-weather"
version = "0.1.0"
description = "A MCP Example Srv"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.8.0",
]
